/** ScreenProgress — placeholder for Act 5 progress morph (future). */
export default function ScreenProgress() {
  return (
    <div
      aria-hidden="true"
      style={{ position: 'absolute', inset: 0, background: '#f8fbfe', display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0 }}
    >
      <span style={{ fontSize: '11px', color: '#9ab0be' }}>Progresso</span>
    </div>
  );
}
