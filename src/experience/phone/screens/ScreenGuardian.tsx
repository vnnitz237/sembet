/** ScreenGuardian — placeholder for Act 6 guardian / reinforced protection (future). */
export default function ScreenGuardian() {
  return (
    <div
      aria-hidden="true"
      style={{ position: 'absolute', inset: 0, background: '#f0f6ff', display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0 }}
    >
      <span style={{ fontSize: '11px', color: '#9ab0be' }}>Guardião</span>
    </div>
  );
}
