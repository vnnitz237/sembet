/** ScreenProtection — placeholder for Act 3+ enhanced protection view (future). */
export default function ScreenProtection() {
  return (
    <div
      aria-hidden="true"
      style={{ position: 'absolute', inset: 0, background: '#f8fbfe', display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0 }}
    >
      <span style={{ fontSize: '11px', color: '#9ab0be' }}>Proteção</span>
    </div>
  );
}
