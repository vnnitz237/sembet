/** ScreenSOS — placeholder for Act 4 guided breathing / SOS flow (future). */
export default function ScreenSOS() {
  return (
    <div
      aria-hidden="true"
      style={{ position: 'absolute', inset: 0, background: '#f0f6ff', display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0 }}
    >
      <span style={{ fontSize: '11px', color: '#9ab0be' }}>Apoio</span>
    </div>
  );
}
